services:
  ksp-v8:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/amd64
    ports:
      - "9000:9000"
    environment:
      X_ZOHO_CATALYST_LISTEN_PORT: "9000"
      ENABLE_PIPELINE_RUN: "0"
      ADMIN_TOKEN: "change-this-before-enabling-runs"
